# since this isn't a Helm-based kapp, we have to track the version somewhere
# other than a `Chart.yaml` file, so we put it here...
version: 0.1.0
interfaces:     # this kapp needs to be parameterised as a k8s_resource kapp
- k8s_resource
- helm