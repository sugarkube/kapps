nginx-ingress:
  controller:
    stats:
      enabled: "true"
    metrics:
      enabled: "true"
    service:
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "10254"

    config:
      ssl-redirect: "true"
